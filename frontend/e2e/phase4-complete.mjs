import { chromium } from 'playwright';
const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1600,height:1000}});await page.goto('http://127.0.0.1:4173/Eternal/',{waitUntil:'networkidle'});await page.click('#phase4-toggle');await page.click('#gain-xp');await page.click('[data-skill="Inspiración real"]');await page.click('[data-skill="Carga del guardián"]');await page.click('[data-tab="RESEARCH"]');for(const id of ['steel','formation','logistics','guilds','runes','crown']){const b=page.locator(`[data-research="${id}"]`);if(await b.isEnabled())await b.click()}
const status=(await page.locator('#phase4-status').textContent())?.trim();if(status!=='FASE IV COMPLETADA')throw new Error(`Fase IV incompleta: ${status}`);await page.screenshot({path:'e2e-artifacts/phase4-complete.png',fullPage:true});console.log('FASE IV OK: héroe nivelado, 2 habilidades y árbol de investigación completado');await browser.close();