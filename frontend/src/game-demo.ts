import './game-demo.css';
import './progression-overlay';
import './campaign-overlay';
import { phase2Collect } from './phase2-overlay';
import './phase3-overlay';
import { ArmyRoster, Economy, TrainingQueue, UNITS, type UnitCode } from './core';
type View='city'|'map'|'hero'|'army'|'research'|'alliance';
const app=document.querySelector<HTMLDivElement>('#app')!;const eco=new Economy({WOOD:12500,FOOD:8700,STONE:6200,GOLD:4100,GEMS:210});const army=new ArmyRoster();army.add('SWORDSMAN',320);army.add('ARCHER',210);army.add('KNIGHT',80);army.add('MAGE',35);const training=new TrainingQueue(eco,army);let selected:UnitCode='SWORDSMAN',view:View='map',research=67,heroXp=77;let log=['Fase III activa: Fronteras de Elyndor.'];const nav:[View,string,string][]=[['city','🏰','CIUDAD'],['map','🗺','MAPA'],['hero','♞','HÉROE'],['army','⚔','EJÉRCITO'],['research','⚗','INVESTIGACIÓN'],['alliance','♢','ALIANZA']];
function header(){const r=eco.snapshot();return `<header><div class="logo">♛ <b>ETERNAL CROWN</b><small>ALPHA · ELYNDOR 0.6 · FASE III</small></div><div class="res"><span>🪵 ${Math.floor(r.WOOD)}</span><span>🌾 ${Math.floor(r.FOOD)}</span><span>⬡ ${Math.floor(r.STONE)}</span><span>● ${Math.floor(r.GOLD)}</span><span>◆ ${Math.floor(r.GEMS)}</span></div><div class="power">⚡ ${army.power().toLocaleString()}</div></header>`}function bottom(){return `<nav>${nav.map(([k,i,n])=>`<button data-view="${k}" class="${view===k?'on':''}">${i}<span>${n}</span></button>`).join('')}</nav>`}function shell(body:string,side:string){return `${header()}<main><section class="world">${body}</section><aside>${side}</aside></main>${bottom()}`}function chronicle(){return `<div class="events"><b>CRÓNICA DEL REINO</b>${log.slice(-6).reverse().map(x=>`<p>› ${x}</p>`).join('')}</div>`}
function city(){return shell(`<div class="city-scene"><div class="sun"></div><div class="ridge r1"></div><div class="ridge r2"></div><div class="river-city"></div><div class="castle3d"><div class="castle-keep">♛</div><div class="tower3d a"></div><div class="tower3d b"></div><div class="wall3d"></div><b>CASTILLO DE ELYNDOR</b></div><div class="city-label"><strong>ELYNDOR</strong><span>Capital preparada para la expansión territorial</span></div></div>`,`<h2>Reino consolidado</h2><p class="muted">La economía de Fase II sostiene ahora las expediciones de conquista.</p><button class="soft" id="collect">RECAUDAR PRODUCCIÓN</button>${chronicle()}`)}
function mapView(){return shell(`<div class="army-scene"><div class="army-banner">🗺</div><h1>FRONTERAS DE ELYNDOR</h1><p>La niebla cubre las provincias orientales</p><div class="formations"><div>🔭<b>Explora</b><span>Descubre amenazas</span></div><div>⚔<b>Conquista</b><span>Expande la Corona</span></div><div>♛<b>Controla</b><span>Obtén bonificaciones</span></div></div></div>`,`<h2>Fase III · Mapa y conquista</h2><p class="muted">Abre el panel CONQUISTA. Explora cada provincia antes de atacar, envía expediciones y elimina la niebla de guerra.</p><div class="card"><b>Objetivo principal</b><p>Controla Bosque de Aelwyn, Ruinas de Vaelor y Torre del Cuervo.</p></div>${chronicle()}`)}
function armyView(){return shell(`<div class="army-scene"><div class="army-banner">⚔</div><h1>EJÉRCITO EXPEDICIONARIO</h1><p>${army.total()} soldados</p></div>`,`<h2>Fuerza de conquista</h2>${(Object.keys(UNITS)as UnitCode[]).map(c=>`<button class="unit ${selected===c?'sel':''}" data-unit="${c}"><span><b>${UNITS[c].name}</b></span><strong>${army.count(c)}</strong></button>`).join('')}<button class="gold" id="train">ENTRENAR 5</button>${chronicle()}`)}
function heroView(){return shell(`<div class="hero-scene"><div class="hero-silhouette">♞</div><div class="hero-name"><h1>ARTHUR</h1><p>Comandante de las expediciones</p></div></div>`,`<h2>Héroe nivel 12</h2><div class="hero-level"><div><i style="width:${heroXp}%"></i></div></div>${chronicle()}`)}function researchView(){return shell(`<div class="research-scene"><h1>CARTOGRAFÍA Y LOGÍSTICA</h1></div>`,`<h2>Logística militar</h2><div class="big-progress"><i style="width:${research}%"></i></div><button class="gold" id="research">INVESTIGAR +12%</button>${chronicle()}`)}function allianceView(){return shell(`<div class="alliance-scene"><div class="crest">♛</div><h1>GUARDIANES DE ELYNDOR</h1></div>`,`<h2>Alianza</h2><p class="muted">Los aliados observan la expansión de tus fronteras.</p>${chronicle()}`)}
function render(){app.innerHTML=view==='city'?city():view==='map'?mapView():view==='hero'?heroView():view==='army'?armyView():view==='research'?researchView():allianceView();bind()}function bind(){document.querySelectorAll<HTMLElement>('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view as View;render()});document.querySelectorAll<HTMLElement>('[data-unit]').forEach(b=>b.onclick=()=>{selected=b.dataset.unit as UnitCode;render()});document.querySelector('#collect')?.addEventListener('click',()=>{eco.grant({FOOD:640,WOOD:470,STONE:305});phase2Collect();log.push('Suministros recaudados para las expediciones.');render()});document.querySelector('#train')?.addEventListener('click',()=>{try{const o=training.start(selected,5,Date.now());training.tick(o.finishAt);log.push('Refuerzos entrenados.')}catch(e){log.push((e as Error).message)}render()});document.querySelector('#research')?.addEventListener('click',()=>{research=Math.min(100,research+12);render()})}render();